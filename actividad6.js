//Actividad6-Pares e Impares 

let numero = 0;

for (let i = 1; i <= 20; i++) {
    if (i % 2 === 0) {
        console.log(`${i} es un numero par`);
    } else {
        console.log(`${i} es un numero impar`);
    }
}